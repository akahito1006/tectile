<div class="container">
  <div class="row mb-5">
    <div class="col-8 p-0 mx-auto main-visual text-center d-flex align-items-end">
        <div class="main-visual-content text-light">
          ようこそ、TecTileへ！<br />
          お役立ちガジェット、趣味のIoTの情報投稿サイトです<br />
          会員登録をすると記事の投稿、いいね・コメントができます<br />
          (Gerd AltmannによるPixabayからの画像)
        </div>
    </div>
  </div>
  
  <div class="row mb-3">
    <div class="col">
      <h4>新着記事</h4>
    </div>
  </div>
  <div class="row mb-3 justify-content-around">
    <!--% @articles.each do |f| %>-->
    <% if @articles.present? %>
      <% @articles.each do |article| %>
        <div class="col-3 shadow float">
          <%= link_to article_path(article.id) do %>
            <% article.article_images.first(1).each do |img| %>
              <%= attachment_image_tag img, :image, class:"img-thumbnail", width:"200", fallback: "smart-home_640" %>
            <% end %>
            <h5><%= article.title %></h5>
          <% end %>
          <h5><%= article.user.name %></h5>
          <h5><%= article.likes.size %></h5>
          <h5><%= article.created_at.strftime('%Y/%m/%d') %></h5>
        </div>
      <% end %>
    <% else %>
      <div class="col offset-md-1">
        <h5>投稿記事はありません。</h5>
    　</div>
    <% end %>
  </div>
  
  <div class="row">
    <div class="col">
      <%= link_to "全ての記事を見る", articles_path, class:"h5 fab fa-flipboard float-right" %>
    </div>
  </div>
</div>